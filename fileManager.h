#ifndef FILEMANAGER_H
#define FILEMANAGER_H
#include "Texture.h"
#include "game.h"
#include <string>
#include <iostream>
#include <fstream>
#include <cstring>

bool loadSrc();

bool loadFonts();

void saveProgress(int[]);

#endif // FILEMANAGER_H

